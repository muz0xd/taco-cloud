server:
  port: 9000

management:
  endpoints:
    web:
      exposure:
        include: '*'

     
logging:
  level: 
    org:
      zalando:
        logbook: TRACE

logbook:
  format.style: curl
  filter:
    enabled: false